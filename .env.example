# Optional. Without this, the app falls back to the offline Mock provider.
# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional override — defaults to gemma-4-4b-it. Verify against
# https://ai.google.dev/gemma/docs/core/model_card_4 before relying on it;
# Gemma 4 model ID strings have shifted between release waves.
GEMMA_MODEL_ID=
